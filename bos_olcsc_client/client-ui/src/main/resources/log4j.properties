# An example log4j configuration file that outputs both to System.out
# and a file named 'test'.
#NOTE: Normally only used by Eclipse for embedded test runs and JUnit tests.

# For the general syntax of property based configuration files see the
# documenation of org.apache.log4j.PropertyConfigurator.

# WARNING: Location information can be useful but is very costly in
# terms of computation.

# The root logger uses the appender called A1.

# The root logger uses the appenders called A1 and A2. Since no level
# is specified, note the empty string between the comma (",") and the
# equals sign ("="), the level of the root logger remains
# untouched. Log4j always initializes the level for the root logger to
# DEBUG. The root logger is the only logger that has a default
# level. Bu default, all other loggers do not have an assigned level,
# such that they inherit their level instead.

#log4j.threshold=INFO, ERROR
#log4j.threshold=INFO
log4j.rootLogger=INFO, A1, A2
#log4j.rootLogger=, A2

# A1 is set to be ConsoleAppender sending its output to System.out
log4j.appender.A1=org.apache.log4j.ConsoleAppender


# A1 uses PatternLayout.
log4j.appender.A1.layout=org.apache.log4j.PatternLayout

# The conversion pattern consists of date in ISO8601 format, level,
# thread name, logger name truncated to its rightmost two components
# and left justified to 17 characters, location information consisting
# of file name (padded to 13 characters) and line number, nested
# diagnostic context, the and the application supplied message

log4j.appender.A1.layout.ConversionPattern=%d %-5p [%t] %-17c{2} (%13F:%L) %3x - %m%n

#log4j.logger.com.etcc.csc=ALL, A2
#log4j.logger.com.etcc.csc=DEBUG, A2
#log4j.logger.com.etcc.csc=INFO, A2
log4j.logger.com.etcc.csc=INFO, A2

# logging duplicate
log4j.additivity.com.etcc.csc=false


#log4j.appender.A2=org.apache.log4j.FileAppender
log4j.appender.A2.File=/app/oracle/files/olcsc/olcsc.log
log4j.appender.A2=org.apache.log4j.RollingFileAppender
#Every hour new file creating up to 24 log files per day (Each file no size limit).
#log4j.appender.A2.DatePattern='.'yyyy-MM-dd-HH


#Only applicable for RollingFileAppender, not DailyRollingFileAppender
log4j.appender.A2.MaxFileSize=1GB
log4j.appender.A2.MaxBackupIndex=1000

#log4j.appender.A2.layout=org.apache.log4j.TTCCLayout
log4j.appender.A2.Append=true
log4j.appender.A2.layout=org.apache.log4j.PatternLayout
log4j.appender.A2.layout.ConversionPattern=%d %-5r %-5p [%t] %c{2} - %m%n
